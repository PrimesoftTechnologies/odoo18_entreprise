<?xml version="1.0" encoding="UTF-8"?>
<odoo>
    <record id="product_template_form_view_inherit_commission" model="ir.ui.view">
        <field name="name">product.template.form.inherit.commission</field>
        <field name="model">product.template</field>
        <field name="inherit_id" ref="product.product_template_form_view"/>
        <field name="arch" type="xml">
            <xpath expr="//page[@name='pos']" position="inside">
                <group string="Commission Settings">
                    <!-- Zinafanya kazi kwa kupokezana (Ukiwasha moja nyingine inakuwa read-only) -->
                    <field name="is_not_commission" readonly="is_fixed_commission"/>
                    <field name="is_fixed_commission" readonly="is_not_commission"/>
                    <field name="fixed_commission_amount" invisible="not is_fixed_commission" required="is_fixed_commission"/>
                </group>
            </xpath>
        </field>
    </record>
</odoo>